# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Coverage reports (generated locally + in CI)
.coverage
.coverage.*
coverage.xml
htmlcov/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Compiled Swift helpers (rebuilt on first run)
resources/mac-ax-helper
resources/mac-ax-watcher
resources/mac-ax-actuator

# Local Persome data (only if symlinked into the repo for dev)
.persome/

# Ad-hoc logs / scratch
*.log
scratch/

# Mutation testing
mutants/
mutmut_config.py
.env

# Claude Code worktrees (local dev isolation)
.claude/worktrees/
src/persome/meeting/hotwords.txt
resources/mac-audio-capture

# Eval artifacts (generated locally + in CI nightly eval)
eval-report.md
eval-intent-metrics.json

resources/mac-virtual-stage

# Eval harness + eval data (harvested real-usage captures, goldens, baselines,
# scenarios) — team-local, never distributed on GitHub.
# The required PII gate lives in scripts/pii_scan.py (tracked).
tests/eval/
tests/evals/

# Harvested real-capture fixtures (real screen content; names anonymized but
# message text is genuine) — team-local, never distributed on GitHub.
# Their tests skip cleanly when these are absent (see tests/conftest.py).
tests/fixtures/captures/lark/
tests/fixtures/captures/browser/
tests/fixtures/ocr/
