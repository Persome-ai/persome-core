## New window to summarize

Window: {start_time} to {end_time} ({block_count} timeline blocks, {capture_count} raw capture records).

Timeline blocks (your primary evidence):
---
{blocks_text}
---
{attention_text}
