Window: {start_time} to {end_time} ({capture_count} screen-content snapshots from the macOS Accessibility API). Records are ordered chronologically — earlier first.

---
{events_text}
---
{skill_index_section}
